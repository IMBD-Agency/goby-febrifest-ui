{
  "name": "manfare-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Manfare - Men's Fashion E-commerce landing page (HTML + Tailwind CSS)",
  "scripts": {
    "dev": "serve -s . -l 3000",
    "start": "serve -s . -l 3000",
    "build:css": "tailwindcss -i ./assets/css/tailwind.css -o ./assets/css/style.css --minify"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
